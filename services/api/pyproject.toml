[project]
name = "api"
version = "0.1.0"
description = "Python API member of the polyglot monorepo."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api"]
